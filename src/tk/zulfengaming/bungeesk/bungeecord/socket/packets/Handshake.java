package tk.zulfengaming.bungeesk.bungeecord.socket.packets;

import tk.zulfengaming.bungeesk.universal.socket.Packet;
import tk.zulfengaming.bungeesk.universal.socket.PacketHandler;
import tk.zulfengaming.bungeesk.universal.socket.PacketTypes;

public class Handshake extends PacketHandler {

    public Handshake() {
        super(PacketTypes.HANDSHAKE);
    }

    @Override
    public Packet handlePacket(Packet packetIn) {
        Packet packetToSend = new Packet()
        return null;

    }
}
